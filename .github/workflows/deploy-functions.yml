name: Deploy Edge Functions

on:
  push:
    branches: [main]
    paths:
      - "supabase/functions/**"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy Edge Functions
        run: |
          supabase functions deploy create-checkout-session --project-ref ${{ vars.SUPABASE_PROJECT_REF }}
          supabase functions deploy stripe-webhook          --project-ref ${{ vars.SUPABASE_PROJECT_REF }}
          supabase functions deploy whatsapp-webhook        --project-ref ${{ vars.SUPABASE_PROJECT_REF }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
